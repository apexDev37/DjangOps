# Requirements for test runs.
-c constraints.txt

-r base.txt               # Core dependencies for this app.

pytest-cov                # pytest extension for code coverage statistics.
pytest-django             # pytest extension for better Django support.
