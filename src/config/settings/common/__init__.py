"""Common settings for Django project.

This module hosts all common config settings for the project
to be inherited and extended across sub settings and environments.

Note:
    `base` is the current abstract, non-runnable settings module.

Candidate settings:
    - Core Django settings.
    - Configurations shared across target environments.
    - Settings required to be extended or overridden.
"""
