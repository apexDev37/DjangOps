---
version: '3.9'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: django_web
    volumes:
      - ./:/usr/src/app
    networks:
      - app_network
    ports:
      - "8000:8000"

networks:
  app_network:
